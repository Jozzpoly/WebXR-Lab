import './style.css';

const app = document.querySelector('#app');
app.innerHTML = `
  <section class="shell">
    <p class="eyebrow">Fresh foundation</p>
    <h1>Riftworks</h1>
    <p>Engineering sandbox reboot. No inherited gameplay runtime is active.</p>
    <p class="muted">Next: desktop-testable authored → runtime foundation, then direct construction.</p>
  </section>
`;
